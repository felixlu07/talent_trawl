# Anthropic API Configuration
# Copy this file to .env and add your actual API key

ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Poppler Configuration (Optional)
# Only needed if Poppler is not in your system PATH or installed in a custom location
# Windows: Set to the bin directory, e.g., C:\Program Files\poppler-24.08.0\Library\bin
# Linux/Mac: Usually not needed (auto-detected from system PATH via brew/apt)
# POPPLER_PATH=
